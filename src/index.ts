import portal from './portal';
import Modal from './modal';
import ModalView from './view';

const ModalProvider = portal.Provider;

export { ModalView, ModalProvider, portal };

export default Modal;
